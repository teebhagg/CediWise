/**
 * Product feature toggles (mobile app).
 *
 * Prefer flipping these over large comment blocks so intent stays searchable.
 */

/**
 * "Survive the Month" cash-flow UI (balance + run-out / safe-to-spend widget and
 * `/budget/cash-flow` detail). Temporarily off while positioning vs Savings Vault
 * and a clearer value story are defined — re-enable when ready.
 */
export const SURVIVE_THE_MONTH_CASH_FLOW_UI_ENABLED = false;
